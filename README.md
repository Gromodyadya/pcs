**ОТЧЕТ по Практическому занятию №14**
**Тема:** Тестирование и оптимизация мобильного приложения. Исправление ошибок (Flutter).
---

### 1. Цели работы
1.  Освоить инструменты статического анализа кода (`flutter analyze`, `flutter_lints`).
2.  Реализовать Unit и Widget тесты для проверки бизнес-логики и UI.
3.  Изучить инструменты профилирования DevTools (Performance, Memory) для выявления проблем производительности.
4.  Применить практики оптимизации списков (`ListView.builder`, `itemExtent`, `Keys`) и работы с памятью.
5.  Настроить глобальный перехват ошибок и пользовательский экран сбоя.
---

### 2. Линтинг и Тестирование

**2.1. Статический анализ**
В файл `pubspec.yaml` был подключен пакет `flutter_lints`. Запуск команды `flutter analyze` позволил выявить и исправить проблемы стиля кода (отсутствие `const`, типизация).

`[ВСТАВИТЬ СКРИНШОТ: Терминал с выводом flutter analyze]`

**2.2. Unit и Widget тесты**
В файле `test/widget_test.dart` были реализованы тесты:
*   **Unit-тест:** Проверяет корректность генерации массива данных (длина и содержимое).
*   **Widget-тест:** Проверяет наличие заголовка, списка `ListView` и кнопок управления.

`[ВСТАВИТЬ СКРИНШОТ: Результат успешного прохождения тестов (All tests passed)]`

*Фрагмент кода тестов:*
```dart
testWidgets('App renders list and buttons', (WidgetTester tester) async {
  await tester.pumpWidget(const MyApp());
  await tester.pump();
  
  // Проверка заголовка и наличия списка
  expect(find.text('Быстрый список (Optimized)'), findsOneWidget);
  expect(find.byType(ListView), findsOneWidget);
});
```

---

### 3. Профилирование «ДО» (Исходное состояние)

Для анализа была создана неоптимизированная версия приложения: список из 1000 элементов рендерился через обычный конструктор `ListView`, что приводило к созданию всех виджетов сразу.

**Показатели DevTools:**
1.  **Memory:** Высокое потребление памяти (~400 МБ) из-за удержания всех 1000 элементов в оперативной памяти.
2.  **Performance:** При прокрутке зафиксированы значительные задержки (Jank) и красные полосы на графике (время кадра > 16 мс).

`[ВСТАВИТЬ СКРИНШОТ: График Memory из DevTools (высокий уровень)]`

`[ВСТАВИТЬ СКРИНШОТ: График Performance из DevTools (с красными столбиками)]`

---

### 4. Перечень оптимизаций

Для устранения проблем была проведена комплексная оптимизация кода:

| № | Оптимизация | Зачем применена | Как реализовано | Эффект |
|:---|:---|:---|:---|:---|
| 1 | **ListView.builder** | Обычный `ListView` создавал 1000 виджетов сразу. | Замена на ленивый конструктор `builder`. | В памяти хранятся только видимые элементы (~38 шт). |
| 2 | **itemExtent** | Flutter тратил время на вычисление высоты каждого элемента. | Установлен параметр `itemExtent: 80`. | Снижена нагрузка на CPU при расчете макета (Layout). |
| 3 | **const конструкторы** | При каждом кадре создавались дубликаты виджетов. | Добавлен `const` к текстам, иконкам и `ItemWidget`. | Уменьшено количество аллокаций памяти (Garbage Collector отдыхает). |
| 4 | **Декомпозиция** | Перерисовка списка вызывала пересчет всего экрана. | Элемент списка вынесен в отдельный класс `ItemWidget`. | Изоляция контекста перерисовки (rebuild scope). |
| 5 | **ValueKey** | Потеря состояния элементов при изменениях дерева. | Добавлен `key: ValueKey(index)`. | Корректная идентификация элементов виджетов. |

*Фрагмент оптимизированного кода:*
```dart
ListView.builder(
  itemCount: items.length,
  itemExtent: 80, // Фиксированная высота
  itemBuilder: (context, index) {
    return ItemWidget(
      key: ValueKey(index), // Уникальный ключ
      text: items[index],
    );
  },
)
```

---

### 5. Профилирование «ПОСЛЕ»

После внесения изменений проведены повторные замеры в режиме `flutter run --profile`.

**Результаты:**
1.  **Memory:** Количество активных экземпляров `ItemWidget` в памяти снизилось с 1000 до ~38 (только видимая область). График памяти стабилизировался.
2.  **Performance:** График отрисовки стал равномерным, отсутствуют пропуски кадров (Jank), достигнута плавность 60 FPS.

`[ВСТАВИТЬ СКРИНШОТ: Таблица Memory (где ItemWidget count около 38)]`

`[ВСТАВИТЬ СКРИНШОТ: График Performance (синий/зеленый, без лагов)]`

---

### 6. Анализ размера приложения

Выполнен анализ размера релизной сборки командой:
`flutter build apk --release --target-platform android-arm64 --analyze-size`

*   **Итоговый размер:** ~14.4 МБ (стандартный размер движка Flutter).
*   **Меры по оптимизации размера:**
    1.  Сборка выполнена конкретно под архитектуру **android-arm64**, что исключило лишние бинарные файлы (x86, arm v7) и уменьшило итоговый APK.
    2.  Автоматически применен **Tree-shaking** для шрифтов и иконок (как видно из лога сборки, шрифт MaterialIcons уменьшен на 99.9%).

`[ВСТАВИТЬ СКРИНШОТ: Таблица из терминала с анализом размера]`

---

### 7. Обработка ошибок

Реализована двухуровневая система обработки ошибок:
1.  **Глобальный перехват:** Использован `runZonedGuarded` для логгирования асинхронных ошибок.
2.  **UI-перехват:** Переопределен `ErrorWidget.builder`. Вместо системного "красного экрана" пользователь видит дружелюбное сообщение "Ой! Произошла ошибка".

Для демонстрации работы была добавлена кнопка, принудительно вызывающая исключение в методе `build`.

*Фрагмент кода:*
```dart
builder: (context, widget) {
  ErrorWidget.builder = (FlutterErrorDetails errorDetails) {
    return const Scaffold(
      body: Center(child: Text("Ой! Произошла ошибка :(")),
    );
  };
  return widget!;
},
```

`[ВСТАВИТЬ СКРИНШОТ: Экран телефона с белым фоном и текстом ошибки]`

---

### 8. Выводы

В ходе выполнения лабораторной работы я освоил процесс оптимизации Flutter-приложений.
Ключевые выводы:
1.  Использование **ListView.builder** является обязательным для списков любой значимой длины, так как это снижает потребление памяти в десятки раз.
2.  Инструменты **DevTools** (Performance и Memory) позволяют объективно оценить эффективность кода, выявляя проблемы, которые могут быть незаметны на мощных устройствах разработчика.
3.  Применение **const** конструкторов — самый простой и эффективный способ снизить нагрузку на Garbage Collector.
4.  Качественная обработка ошибок (Custom Error Widget) значительно улучшает пользовательский опыт в критических ситуациях.
